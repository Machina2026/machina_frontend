# Copy to .env.local if you need local overrides. Never commit real secrets.
#
# No variables are required today: the app runs on the built-in mock API
# (src/app/api/[...path]), served from the same origin.
